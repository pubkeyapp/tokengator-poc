export const cliPageOverview = `
# Overview

This is the overview of the \`tokengator\` CLI.

## Usage

Currently, the CLI is not distributed as a package.

You can run the CLI by cloning the repository and running the following command:

\`\`\`bash
$ git clone https://github.com/pubkeyapp/tokengator.git
$ cd tokengator
$ pnpm install
$ pnpm run tokengator --help
\`\`\`

`
